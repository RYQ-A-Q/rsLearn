import { Enum } from "cc";

export enum SceneEnum {
    start = "start",
    uiScene = "uiScene",
    funcScene = "funcScene",
    gameScene = "gameScene",
    designModeScene = "designModeScene"
}
Enum(SceneEnum)